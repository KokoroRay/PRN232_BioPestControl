http:
  routers:
    k3s-frontend:
      rule: "PathPrefix(`/`)"
      service: "k3s-service"
      entryPoints:
        - "web"
  services:
    k3s-service:
      loadBalancer:
        servers:
          - url: "http://10.0.0.2:80"
