apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-service
  namespace: biopest
spec:
  replicas: 1
  selector:
    matchLabels:
      app: identity-service
  template:
    metadata:
      labels:
        app: identity-service
    spec:
      containers:
        - name: identity-service
          image: 10.0.0.3:5000/identity-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: identity-service-env
          env:
            - name: AzureSql__ConnectionString
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: connectionString
            - name: JwtSettings__Key
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: jwtKey
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 6
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 2
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: biopest
spec:
  selector:
    app: identity-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
